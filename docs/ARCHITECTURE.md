# 🏗️ 아키텍처 설계

## 시스템 개요

My Routine Adventure(MRA)는 **MVC 패턴**을 변형한 구조로 설계되었습니다. 핵심 비즈니스 로직은 `RoutineManager`에 집중되어 있으며, 각 UI 컴포넌트는 매니저를 통해 데이터에 접근합니다.

## 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────────────┐
│                        MainDashboard                            │
│                    (메인 윈도우 / 진입점)                          │
│  ┌─────────┐  ┌─────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │대시보드탭│  │스트릭탭 │  │ 통계탭   │  │ 루틴 관리 패널   │  │
│  └─────────┘  └─────────┘  └──────────┘  └──────────────────┘  │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      RoutineManager                             │
│                   (핵심 비즈니스 로직)                            │
│  • 루틴 CRUD                                                    │
│  • 루틴 완료 처리 및 보상 지급                                    │
│  • 보스 데미지 계산                                              │
│  • 데이터 저장/로드 조율                                         │
└───────────────────────────┬─────────────────────────────────────┘
                            │
            ┌───────────────┼───────────────┬───────────────┐
            ▼               ▼               ▼               ▼
    ┌───────────┐   ┌───────────┐   ┌───────────┐   ┌───────────┐
    │  Player   │   │  Routine  │   │   Boss    │   │   Item    │
    │ (플레이어)│   │  (루틴)   │   │  (보스)   │   │ (아이템)  │
    └───────────┘   └───────────┘   └───────────┘   └───────────┘
            │               │               │               │
            └───────────────┴───────────────┴───────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │  FileManager  │
                    │ (파일 입출력) │
                    └───────────────┘
                            │
            ┌───────────────┼───────────────┐
            ▼               ▼               ▼
    routines_data.txt  player_data.dat  boss_data.txt
```

## 레이어 구조

### 1. Presentation Layer (UI)
화면 표시 및 사용자 상호작용을 담당합니다.

| 클래스 | 역할 |
|--------|------|
| `MainDashboard` | 메인 윈도우, 탭 패널 관리, 프로그램 진입점 |
| `RoutineListView` | 요일별 루틴 목록 표시 (탭 기반) |
| `RoutineManagerGUI` | 루틴 생성 폼 |
| `RoutineModify` | 루틴 수정 다이얼로그 |
| `InventoryView` | 인벤토리 및 장비 관리 UI |
| `ShopView` | 아이템 상점 UI |
| `StreakWindow` | 연속 달성 달력 표시 |
| `StatisticsPanel` | 월간 통계 및 그래프 |

### 2. Business Logic Layer (매니저)
핵심 비즈니스 로직을 처리합니다.

| 클래스 | 역할 |
|--------|------|
| `RoutineManager` | 루틴 관련 모든 비즈니스 로직의 중앙 허브 |
| `ItemDropManager` | 아이템 드랍 확률 및 보상 결정 |
| `DateUtil` | 시스템 시간 조회 유틸리티 |

### 3. Model Layer (데이터 모델)
데이터 구조를 정의합니다.

| 클래스 | 역할 |
|--------|------|
| `Routine` | 루틴 데이터 (이름, 태그, 반복요일, 완료상태) |
| `Player` | 플레이어 데이터 (레벨, 경험치, 골드, 인벤토리) |
| `Boss` | 보스 데이터 (월별 컨셉, HP, 스토리) |
| `Item` | 아이템 데이터 (이름, 효과, 가격, 슬롯) |
| `Inventory` | 아이템 저장 및 장비 슬롯 관리 |

### 4. Persistence Layer (저장)
데이터 영속성을 관리합니다.

| 클래스 | 역할 |
|--------|------|
| `FileManager` | 텍스트 파일 및 직렬화 기반 저장/로드 |

## 클래스 간 의존 관계

```
MainDashboard ─────┬──────────────────────────────────────┐
    │              │                                      │
    │ has-a        │ has-a                                │
    ▼              ▼                                      ▼
RoutineManager   Player                                 Boss
    │              │                                      │
    │ manages      │ has-a                                │
    ▼              ▼                                      │
 Routine        Inventory ◄────────────────────────────────┘
    │              │
    │              │ contains
    │              ▼
    │            Item
    │
    │ uses
    ▼
FileManager ──────► 파일 시스템
```

## 이벤트 흐름

### 루틴 완료 시 처리 흐름

```
1. 사용자가 RoutineListView에서 체크박스 클릭
                    │
                    ▼
2. TableModelListener가 변경 감지
                    │
                    ▼
3. RoutineManager.completeRoutine(id, day) 호출
                    │
    ┌───────────────┼───────────────┐
    ▼               ▼               ▼
4. Routine       Player          Boss
   .completeForDay()  .gainExp()     .takeDamage()
                      .gainGold()
                    │
                    ▼
5. ItemDropManager.dropItem() → 확률적 아이템 획득
                    │
                    ▼
6. FileManager.saveRoutinesToFile() → 자동 저장
   FileManager.savePlayerState()
   FileManager.saveBossState()
                    │
                    ▼
7. MainDashboard.updatePlayerStatusUI()
   MainDashboard.updateBossUI()
   MainDashboard.addLogMessage()
```

## 데이터 저장 전략

### 텍스트 저장 (루틴)
- 파일: `routines_data.txt`
- 형식: 파이프(|) 구분 텍스트
- 장점: 사람이 읽기 쉬움, 디버깅 용이
- 용도: 루틴 데이터 (단순 구조)

### 직렬화 저장 (플레이어, 보스)
- 파일: `player_data.dat`, `boss_data.txt`
- 형식: Java ObjectOutputStream
- 장점: 복잡한 객체 구조 보존 (인벤토리, 장비 Map 등)
- 용도: 플레이어, 보스 데이터 (복잡 구조)

## 확장 포인트

### 1. 새로운 아이템 효과 추가
`Item.EffectType` enum에 새 상수 추가 후, `Player.getTotalBonus*()` 메서드 수정

### 2. 새로운 보스 추가
`Boss.setMonthlyConcept()` switch문에 월별 컨셉 추가

### 3. 새로운 통계 지표 추가
`StatisticsPanel.calculateTagStats()` 수정 또는 새 메서드 추가

### 4. 새로운 UI 탭 추가
`MainDashboard.initUI()`의 `tabbedPane.addTab()` 호출 추가
