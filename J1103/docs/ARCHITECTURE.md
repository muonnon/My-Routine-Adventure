# 아키텍처 문서

## 📐 전체 시스템 아키텍처

My-Routine-Adventure는 **MVC(Model-View-Controller)** 패턴을 기반으로 설계되었습니다.

```
┌─────────────────────────────────────────────────────────────────────┐
│                           PRESENTATION LAYER                        │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐   │
│  │MainDashboard│ │RoutineList │ │InventoryView│ │  ShopView   │   │
│  │    (Main)   │ │    View    │ │             │ │             │   │
│  └──────┬──────┘ └──────┬─────┘ └──────┬──────┘ └──────┬──────┘   │
│         │               │              │               │           │
│  ┌──────┴───────────────┴──────────────┴───────────────┴──────┐   │
│  │                    Supporting Views                         │   │
│  │ RoutineManagerGUI │ RoutineModify │ StatisticsPanel │ etc. │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                           BUSINESS LAYER                            │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐     │
│  │ RoutineManager  │  │ ItemDropManager │  │   FileManager   │     │
│  │ (Core Logic)    │  │ (Drop Logic)    │  │  (Persistence)  │     │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘     │
└───────────┼────────────────────┼────────────────────┼───────────────┘
            │                    │                    │
            ▼                    ▼                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                            DATA LAYER                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │  Player  │ │ Routine  │ │   Boss   │ │   Item   │ │Inventory │  │
│  │  (Model) │ │  (Model) │ │  (Model) │ │  (Model) │ │  (Model) │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
│                                                                     │
│                    ┌─────────────────────────┐                      │
│                    │    Text File Storage    │                      │
│                    │  (player_data.txt,     │                      │
│                    │   routines_data.txt)    │                      │
│                    └─────────────────────────┘                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 컴포넌트 상호작용

### 1. 시작 흐름 (Startup Flow)

```
┌──────────────────┐
│ MainDashboard    │
│    main()        │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐     ┌──────────────────┐
│ RoutineManager   │────▶│  FileManager     │
│  (생성자)         │     │ loadRoutines()   │
└────────┬─────────┘     └────────┬─────────┘
         │                        │
         │                        ▼
         │              ┌──────────────────┐
         │              │routines_data.txt │
         │              └──────────────────┘
         ▼
┌──────────────────┐     ┌──────────────────┐
│ FileManager      │────▶│ player_data.txt  │
│ loadPlayerState()│     └──────────────────┘
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│    Player        │
│   (생성/로드)     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  initUI()        │
│ (UI 초기화)       │
└──────────────────┘
```

### 2. 루틴 완료 흐름 (Routine Complete Flow)

```
┌──────────────────┐
│ RoutineListView  │
│ (체크박스 클릭)   │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ RoutineManager   │
│completeRoutine() │
└────────┬─────────┘
         │
    ┌────┴────────────────────────────────┐
    │                                     │
    ▼                                     ▼
┌──────────────────┐            ┌──────────────────┐
│   Routine        │            │   Player         │
│ completeForDay() │            │ gainExpAndGold() │
└──────────────────┘            │ updateStreak()   │
                                └────────┬─────────┘
                                         │
                                         ▼
                                ┌──────────────────┐
                                │ ItemDropManager  │
                                │   dropItem()     │
                                └────────┬─────────┘
                                         │
                            ┌────────────┴────────────┐
                            │ 드랍 성공?              │
                            └────────────┬────────────┘
                                         │ Yes
                                         ▼
                                ┌──────────────────┐
                                │   Inventory      │
                                │   addItem()      │
                                └────────┬─────────┘
                                         │
                                         ▼
                                ┌──────────────────┐
                                │ MainDashboard    │
                                │updatePlayerUI()  │
                                │addLogMessage()   │
                                └──────────────────┘
```

---

## 📊 클래스 다이어그램 (간략)

```
┌─────────────────────────────────────────────────────────────────────┐
│                            <<GUI Classes>>                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  MainDashboard ◄──────┬──────────┬───────────┬────────────────┐    │
│       │               │          │           │                │    │
│       │         RoutineList  InventoryView ShopView    StreakWindow│
│       │            View                                            │
│       │               │                                            │
│       │         ┌─────┴─────┐                                      │
│       │    RoutineManager  RoutineModify                           │
│       │        GUI                                                 │
│       │                                                            │
└───────┼────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         <<Manager Classes>>                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   RoutineManager ◄──────────────┐                                   │
│         │                       │                                   │
│         ├──── FileManager       │                                   │
│         │                       │                                   │
│         └──── ItemDropManager   │                                   │
│                                 │                                   │
└─────────────────────────────────┼───────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          <<Model Classes>>                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│    Player ◄────────── Inventory ◄────────── Item                    │
│       │                    │                  │                     │
│       │                    │            ┌─────┴─────┐               │
│       │                    │       EquipSlot    EffectType          │
│       │                    │       (Enum)       (Enum)              │
│       │                    │                                        │
│    Routine ◄──── Boss                                               │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔗 클래스 의존성 관계

| 클래스 | 의존하는 클래스 |
|--------|----------------|
| `MainDashboard` | `RoutineManager`, `Player`, `FileManager`, `RoutineListView`, `InventoryView`, `StreakWindow`, `StatisticsPanel` |
| `RoutineManager` | `Routine`, `Player`, `FileManager`, `ItemDropManager`, `MainDashboard` |
| `Player` | `Inventory`, `Item` |
| `Inventory` | `Item` |
| `RoutineListView` | `RoutineManager`, `Routine`, `RoutineRenderer`, `PopupListener` |
| `InventoryView` | `Player`, `Item`, `Inventory` |
| `ShopView` | `Player`, `Item`, `MainDashboard` |
| `FileManager` | `Routine`, `Player` |

---

## 🎯 설계 원칙

### 1. 단일 책임 원칙 (SRP)
- `FileManager`: 파일 입출력만 담당
- `RoutineManager`: 루틴 비즈니스 로직만 담당
- `ItemDropManager`: 아이템 드랍 로직만 담당

### 2. 의존성 주입
- `MainDashboard`에서 `Player`, `RoutineManager` 객체를 생성하고 다른 컴포넌트에 주입
- `RoutineManager.setPlayer()`, `RoutineManager.setDashboard()` 메서드로 의존성 설정

### 3. 데이터 캡슐화
- `Inventory`, `Player` 등 모델 클래스는 내부 데이터를 직접 노출하지 않고 Getter/Setter를 통해 접근
- `Collections.unmodifiableList()`를 통해 읽기 전용 컬렉션 반환

---

## 📁 패키지 구조

모든 클래스는 `J1103` 패키지에 속해 있습니다.

```
J1103/
├── MainDashboard.java      # Entry Point
│
├── # Model Classes
├── Player.java
├── Routine.java
├── Boss.java
├── Item.java
├── Inventory.java
│
├── # Manager/Service Classes
├── RoutineManager.java
├── FileManager.java
├── ItemDropManager.java
│
├── # View Classes
├── RoutineListView.java
├── RoutineManagerGUI.java
├── RoutineModify.java
├── InventoryView.java
├── ShopView.java
├── StreakWindow.java
├── StatisticsPanel.java
├── SimpleBarChartPanel.java
│
├── # Utility Classes
├── RoutineRenderer.java
├── PopupListener.java
└── PlayerStatusUpdaer.java  # (미구현)
```
